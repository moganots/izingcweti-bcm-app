export enum WidgetType {
  KPI = 'KPI',
  CHART = 'CHART',
  TABLE = 'TABLE',
  LIST = 'LIST',
  CARD = 'CARD',
}

export enum DashboardRole {
  ADMIN = 'ADMIN',
  MANAGER = 'MANAGER',
  USER = 'USER',
}
